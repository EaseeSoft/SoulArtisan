/**
 * 通用节点导出
 * 这些节点可以在多个工作流中共享使用
 */

// 图片展示节点
export { default as ImageDisplayNode } from '../../nodes/ImageDisplayNode';

// 视频展示节点
export { default as VideoDisplayNode } from '../../nodes/VideoDisplayNode';

// 小说输入节点（角色分析）
export { default as RoleAnalysisNode } from '../../nodes/RoleAnalysisNode';

// 剧情描述节点
export { default as StoryDescriptionNode } from '../../nodes/StoryDescriptionNode';
